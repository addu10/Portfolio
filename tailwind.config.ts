import type { Config } from "tailwindcss";

// Empty config since we are not using Tailwind CSS
const config: Config = {
  content: [],
  theme: {
    extend: {},
  },
  plugins: [],
};
export default config; 